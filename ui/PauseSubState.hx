var menuItems:Array<String> = ['Resume', 'Restart Song', 'skiptime', 'Change Difficulty', 'Change Keybinds', 'Options', 'Exit to menu'];

function update() {
    var daSelected:String = menuItems[curSelected];
    switch (daSelected){
        case "skiptime":
            trace("cum");
                
    }
}